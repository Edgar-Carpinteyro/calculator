{
  "name": "cal_node",
  "version": "1.0.0",
  "description": "Calculator project with Node.",
  "main": "index.js",
  "scripts": {
    "build": "cp -r src/ dist/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Edgar Carpinteyro",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
